class Solution {
public:
    int minElement(vector<int>& nums) {
        int mn=INT_MAX;

        for(int i:nums)
            {int cur=0;
            while (i)
                cur+=i%10, i/=10;
            mn=min(mn,cur);}

        return mn;
    }
};
